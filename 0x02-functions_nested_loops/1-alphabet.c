#include "main.h"
/**
 * print_alphabet - prints the english alphabet from a-z.
 * Return: Nothing.
 */
void print_alphabet(void)
{
	char la;

	for (la = 'a'; la <= 'z'; c++)
	{
		_putchar(la);
	}
	_putchar('\n');
}
